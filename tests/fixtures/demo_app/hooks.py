app_name = "demo_app"
app_title = "Demo App"
# A fixture app, deliberately minimal. Outcome probes operate on this rather than on a
# real bench, so a probe run cannot damage anything and cannot depend on site state.
